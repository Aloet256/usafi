# USAFI Capitalbet App

Static Supabase-backed balancing system.

Open locally with:

```bash
python -m http.server 8080
```

Then visit:

```text
http://localhost:8080/
```

For GitHub Pages, publish the repository from the root folder. The live app entry point is `index.html`.
